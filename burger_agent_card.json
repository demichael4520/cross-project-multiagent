{
  "name": "burger-seller-agent",
  "displayName": "Burger Seller Agent",
  "description": "Specialized seller agent for browsing burger menus, checking pricing, and placing orders.",
  "version": "1.0.0",
  "url": "https://us-central1-aiplatform.mtls.googleapis.com/v1/projects/439077346891/locations/us-central1/reasoningEngines/1226814733307346944",
  "protocolBinding": "JSONRPC",
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["text"],
  "capabilities": {
    "streaming": true
  },
  "skills": [
    {
      "id": "get_burger_menu",
      "name": "get_burger_menu",
      "description": "Retrieves the full menu of available burgers and their prices in IDR.",
      "tags": ["menu", "food", "pricing"],
      "parameters": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "id": "create_burger_order",
      "name": "create_burger_order",
      "description": "Places an order for one or more burger menu items and returns an order confirmation ID.",
      "tags": ["order", "food", "burger", "checkout"],
      "parameters": {
        "type": "object",
        "required": ["order_items"],
        "properties": {
          "order_items": {
            "type": "array",
            "description": "List of burger items, quantities, and prices to order.",
            "items": {
              "type": "object",
              "required": ["name", "quantity", "price"],
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Menu item name (e.g. 'Classic Cheeseburger', 'Double Cheeseburger', 'Spicy Chicken Burger', 'Spicy Cajun Burger')."
                },
                "quantity": {
                  "type": "integer",
                  "description": "Number of items."
                },
                "price": {
                  "type": "integer",
                  "description": "Unit price in IDR."
                }
              }
            }
          }
        }
      }
    }
  ]
}
